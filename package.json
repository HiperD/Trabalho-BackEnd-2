{
  "name": "hotel-management-system",
  "version": "1.0.0",
  "description": "Sistema de Gerenciamento de Hotel - Full Stack",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm run install:all && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "seed": "node backend/seed.js",
    "build": "npm run build --prefix frontend"
  },
  "keywords": [
    "hotel",
    "management",
    "fullstack"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.2"
  }
}
